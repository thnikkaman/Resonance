# Self-Test

## Positive triggers

1. “Deduplicate the Local and Streaming toolbar button styles.”
   - Trigger this skill; map placement, action, accessibility, geometry, theme, and gesture differences before parameterizing.
2. “Simplify repeated artist grouping logic.”
   - Trigger this skill; prove local/remote identity, ordering, cache, and Various Artists contracts match only for the shared kernel.
3. “Delete old playback code that looks unused.”
   - Trigger this skill but run the full dead-code gauntlet; do not delete from grep alone.

## Negative triggers

1. “Split PlayerController into files.”
   - Route to `resonance-decompose-isomorphically`.
2. “Make first Streaming load faster.”
   - Route to `resonance-profile-performance` first.
3. “Test real byte ranges and downloads.”
   - Route to `resonance-real-service-e2e`.

## Contract checks

A correct run must create the isomorphism card before editing, use one lever per commit, flag removed surface/events/routes, preserve test collection and output goldens, and refuse aesthetic-only cleanup when equivalence is uncertain.
